---
import "@fontsource/jetbrains-mono/latin-400.css";
import "@fontsource/jetbrains-mono/latin-500.css";
import "../styles/main.scss";

interface Props {
  title: string;
  description: string;
  windowTitle?: string;
}

const {
  title,
  description,
  windowTitle = "valentin@portfolio: ~",
} = Astro.props;

const sourceUrl = "https://github.com/valentin-grenier/portfolio";
const year = new Date().getFullYear();
---

<!--
  hi, dev. you found the source.
  built with astro · scss · jetbrains mono
  https://github.com/valentin-grenier/portfolio
-->
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="dark" />
    <meta name="description" content={description} />
    <meta name="author" content="Valentin Grenier" />
    <meta name="theme-color" content="#000000" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <!-- Privacy-friendly analytics by Plausible -->
    <script
      async
      src="https://plausible.io/js/pa-znNTldu93oV0oqoHyVq0p.js"></script>
    <script>
      window.plausible =
        window.plausible ||
        function () {
          (plausible.q = plausible.q || []).push(arguments);
        };
      plausible.init = plausible.init || function (i) { plausible.o = i || {}; };
      plausible.init();
    </script>
    <title>{title}</title>
  </head>
  <body>
    <main class="terminal" aria-label="Terminal window">
      <header class="terminal__chrome" aria-hidden="true">
        <span class="dots">
          <span class="dot dot--close"></span>
          <span class="dot dot--min"></span>
          <span class="dot dot--max"></span>
        </span>
        <span class="terminal__title">{windowTitle}</span>
        <span class="dots dots--spacer"></span>
      </header>

      <div class="terminal__body">
        <slot />
      </div>
    </main>

    <footer class="foot">
      <span>© {year}</span>
      <span class="sep" aria-hidden="true">·</span>
      <a href={sourceUrl} target="_blank" rel="noopener noreferrer" class="src">
        source
        <span class="sr-only">(opens in a new tab)</span>
      </a>
      <span class="sep" aria-hidden="true">·</span>
      <a href="/privacy" class="src">privacy</a>
    </footer>

    <script is:inline>
      console.log(
        "%cvalentin grenier %c· web developer\n%csource: https://github.com/valentin-grenier/portfolio",
        "color:#4d65c7;font-weight:700;font-size:13px;font-family:ui-monospace,monospace;",
        "color:#9a9a9a;font-family:ui-monospace,monospace;",
        "color:#767676;font-family:ui-monospace,monospace;"
      );
    </script>
  </body>
</html>
